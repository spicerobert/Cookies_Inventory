# 餅乾庫存算料系統 - 打包說明

## 前置需求

1. **安裝 Python 3.12 或以上版本**

2. **建立並啟動虛擬環境（使用 UV）**

   本專案使用 UV 和 pyproject.toml 管理依賴套件。建議使用 UV 建立虛擬環境：
   
   **建立虛擬環境：**
   ```bash
   uv venv --python 3.12 --seed --link-mode=symlink --clear .venv
   ```
   
   **啟動虛擬環境：**
   
   **Windows (Git Bash / CMD):**
   ```bash
   source .venv/Scripts/activate
   ```
   
   **Windows (PowerShell):**
   ```powershell
   .venv\Scripts\Activate.ps1
   ```
   
   **Linux/macOS:**
   ```bash
   source .venv/bin/activate
   ```
   
   **安裝專案依賴套件：**
   ```bash
   uv pip install -e .
   ```
   
   這會自動從 `pyproject.toml` 安裝所有必要的套件，包括：
   - `gspread>=5.12.0`
   - `google-auth>=2.23.0`
   - `google-auth-oauthlib>=1.1.0`
   - `google-auth-httplib2>=0.1.1`
   - `pyodbc>=5.3.0`
   - 以及其他所有依賴
   
   **安裝 PyInstaller（用於打包）：**
   ```bash
   uv pip install pyinstaller
   ```
   
   **替代方式：不使用虛擬環境（直接使用 UV）**
   
   如果不想手動啟動虛擬環境，也可以直接使用 `uv run` 命令：
   ```bash
   uv run pyinstaller build_exe.spec
   ```
   
   這會自動在 UV 管理的虛擬環境中執行命令。

## GUI 圖形界面功能

打包後的 `餅乾庫存算料系統.exe` 提供以下功能：

1. **同步 Index 資料** - 從 ERP 同步餅乾、禮盒等基本資料到 Google Sheets
2. **同步帳上庫存** - 從 ERP 同步餅乾庫存資料
3. **同步在製品庫存** - 從 ERP 同步在製品庫存資料
4. **同步完工入庫** - 從 ERP 同步完工入庫資料到 Google Sheets
5. **同步生產排程** - 從 Google Sheets 讀取生產排程並計算生產片數
6. **計算庫存預估** - 計算未來 21 天的庫存預估和負庫存警示

所有功能都透過圖形界面操作，執行過程會顯示在日誌視窗中。

## 系統模組架構與功能說明

### 核心模組

#### `cookie_inventory_gui.py`
- **功能**：GUI 圖形界面主程式
- **說明**：提供圖形化使用者界面，整合所有同步和計算功能
- **位置**：專案根目錄

#### `cookies_inventory/__init__.py`
- **功能**：套件初始化檔案
- **說明**：定義套件對外介面，匯出所有可用的模組和函數

### 共用工具模組

#### `cookies_inventory/sync_utils.py` ⭐ **新增共用工具模組**
- **功能**：同步模組共用工具函數庫
- **主要功能**：
  - `convert_qty_to_float()` - 數量轉換函數（將 Decimal 或其他類型轉為 float）
  - `get_cookie_codes_from_index()` - 從 Index 工作表取得餅乾代號集合
  - `write_worksheet_data()` - 批次寫入資料到 Google Sheets（支援先清空後寫入）
  - `get_update_date()` - 取得格式化的當前日期時間字串
  - `sync_with_error_handling()` - 統一的錯誤處理包裝器
  - `filter_data_by_index()` - 過濾資料，只保留 Index 中存在的餅乾代號
  - `format_date_yyyymmdd_to_slash()` - 日期格式化（YYYYMMDD → YYYY/MM/DD）
  - 統一的 logging 設定
- **重構說明**：此模組提取了所有同步模組中的重複程式碼，簡化了程式結構並提高可維護性

### Google Sheets 與 ERP 連接模組

#### `cookies_inventory/google_sheets_helper.py`
- **功能**：Google Sheets API 操作輔助類別
- **主要功能**：
  - 連接到 Google Sheets
  - 讀寫工作表資料
  - 管理工作表結構
  - 讀取 Index 工作表的代號對應關係

#### `cookies_inventory/erp_db_helper.py`
- **功能**：ERP 資料庫連接輔助類別
- **主要功能**：
  - 連接到 SQL Server 資料庫
  - 執行 SQL 查詢
  - 提供各類 ERP 資料查詢方法

### 同步功能模組

#### `cookies_inventory/sync_index_from_erp.py`
- **功能**：同步 Index 工作表資料（品名、生重、熟重）
- **說明**：
  - 從 ERP 系統（INVMB 表）查詢餅乾、禮盒、產線的基本資訊
  - 更新 Index 工作表的「名稱」、「生重」、「熟重」欄位
  - 只更新已存在的代號，不會新增或刪除資料行
  - 自動確保標題行包含所有必要欄位
- **重構說明**：使用共用工具模組的 `write_worksheet_data()` 函數

#### `cookies_inventory/sync_inventory_from_erp.py`
- **功能**：同步餅乾帳上庫存資料
- **說明**：
  - 只同步 Index 工作表中存在的餅乾代號
  - 從 ERP 系統查詢 SP40, SP50, SP60, SP80 四個庫別的庫存
  - 合併期初庫存（INVLC）和每日進出數量（INVLA）計算即時庫存
  - 自動將庫存數量統一轉換為「片」為單位（根據餅乾代號最後一碼換算）
  - **每次同步會先刪除所有舊資料，再寫入最新的清單**
  - 支援多庫別，每筆資料包含餅乾代號、品名、庫存數量、庫別代號
- **重構說明**：
  - 使用共用工具模組的 `get_cookie_codes_from_index()`, `filter_data_by_index()`, `write_worksheet_data()`, `convert_qty_to_float()`
  - 移除重複的程式碼和未使用的函數（`build_row_mapping()`, `ensure_headers()`）
  - 簡化錯誤處理邏輯

#### `cookies_inventory/sync_wip_from_erp.py`
- **功能**：同步在製品庫存資料
- **說明**：
  - 只同步 Index 工作表中存在的餅乾代號
  - 從製令單頭（MOCTA）查詢生產中的在製品數量
  - 計算邏輯：已領數量（TA016）- 已生產數量（TA017）
  - 查詢條件：開單日期 >= 20251101 且狀態碼為 '3'（生產中）
  - **每次同步會先刪除所有舊資料，再寫入最新的清單**
  - 每筆資料包含餅乾代號、餅乾品名、製令單別、製令單號、在製品數量
- **重構說明**：
  - 使用共用工具模組的 `get_cookie_codes_from_index()`, `filter_data_by_index()`, `write_worksheet_data()`, `convert_qty_to_float()`, `get_update_date()`
  - 移除重複的過濾和數量轉換函數
  - 簡化資料處理流程

#### `cookies_inventory/sync_receipt_from_erp.py`
- **功能**：同步完工入庫資料
- **說明**：
  - 查詢入庫單表頭（MOCTF）和單身（MOCTG）的合併資料
  - 查詢條件：
    - TF003（入庫日期）在最近 5 天內
    - TF011='P104'
    - TF001 IN ('5801', '5802')
  - **每次同步會先刪除所有舊資料，再寫入最新的清單**
  - 同一天同一餅乾的多筆入庫會自動合併驗收數量
  - 資料依入庫日期降序、餅乾代號升序排列
- **重構說明**：
  - 使用共用工具模組的 `format_date_yyyymmdd_to_slash()`, `convert_qty_to_float()`, `write_worksheet_data()`, `get_update_date()`
  - 移除重複的日期格式化函數
  - 簡化資料合併和排序邏輯

#### `cookies_inventory/sync_production_schedule.py`
- **功能**：重新計算生產排程的生產片數
- **說明**：
  - 從 Google Sheets 的「生產排程」工作表讀取資料
  - 從 Index 工作表讀取生重資料
  - 從 ERP 查詢餅乾品名（INVMB.MB002）
  - 計算公式：生產片數 = 生產顆數 × 160000 ÷ 生重 × 0.95（考慮 5% 損耗）
  - 自動更新名稱欄位和生產片數欄位
  - 按照標準順序重新排列欄位並更新回 Google Sheets
- **重構說明**：
  - 使用共用工具模組的 `write_worksheet_data()` 函數
  - 統一 logging 設定

### 計算功能模組

#### `cookies_inventory/calculate_cookie_inventory.py`
- **功能**：計算未來 21 天的庫存預估和負庫存警示
- **說明**：
  - 從「實盤庫存」工作表讀取今天的期初庫存
  - 讀取 BOM 表、生產排程、組裝排程
  - 計算未來 21 天每一天每種餅乾的庫存數量
  - 檢測庫存不足的情況（包含在「庫存預估明細」工作表的「是否庫存不足」和「缺口數量」欄位）
  - 輸出結果到「庫存預估明細」工作表
  - 警示餅乾庫存不足的情況（當天組裝需求量 > 期初庫存）
- **重構說明**：
  - 統一使用共用工具模組的 logging 設定

## 重構改進說明

### 程式碼優化（2024 年重構）

本次重構主要改進：

1. **創建共用工具模組** (`sync_utils.py`)
   - 提取所有同步模組中的重複程式碼
   - 統一錯誤處理模式
   - 統一 logging 設定格式
   - 減少程式碼重複約 200+ 行

2. **簡化同步邏輯**
   - 所有同步模組改為「先刪除舊資料，再寫入新資料」的模式
   - 避免舊資料殘留問題
   - 簡化資料合併邏輯

3. **統一函數命名和介面**
   - 統一的資料過濾函數 `filter_data_by_index()`
   - 統一的資料寫入函數 `write_worksheet_data()`
   - 統一的日期格式化函數 `format_date_yyyymmdd_to_slash()`

4. **提高程式碼可維護性**
   - 減少重複程式碼
   - 統一錯誤處理
   - 統一日誌格式
   - 更清晰的模組職責劃分

## 打包步驟

### 方法一：使用 spec 檔案（推薦）

1. 確保所有必要檔案都在專案目錄中：
   - `cookie_inventory_gui.py` - 主程式（GUI 圖形界面）
   - `config.ini` - 設定檔
   - `service_account.json` - Google 服務帳戶憑證
   - `cookies_inventory/` - 套件目錄，包含：
     - `__init__.py` - 套件初始化檔案
     - `sync_utils.py` - 同步模組共用工具（⭐ 新增）
     - `sync_index_from_erp.py` - 同步 Index 資料模組
     - `sync_inventory_from_erp.py` - 同步帳上庫存模組
     - `sync_wip_from_erp.py` - 同步在製品庫存模組
     - `sync_production_schedule.py` - 同步生產排程模組
     - `sync_receipt_from_erp.py` - 同步完工入庫模組
     - `calculate_cookie_inventory.py` - 計算庫存預估模組
     - `google_sheets_helper.py` - Google Sheets 操作輔助模組
     - `erp_db_helper.py` - ERP 資料庫連接輔助模組
     - `cookie_inventory_gui.py` - GUI 界面實作

2. **確認已啟動虛擬環境**（如果使用手動啟動方式）

   如果使用 `source .venv/Scripts/activate` 啟動虛擬環境，請確認命令提示字元前面有 `(.venv)` 標記。

3. 執行打包命令：
   ```bash
   pyinstaller build_exe.spec
   ```
   
   或使用 UV 直接執行（不需要手動啟動虛擬環境）：
   ```bash
   uv run pyinstaller build_exe.spec
   ```

4. 打包完成後，可執行檔案會在 `dist/` 目錄中

### 方法二：使用命令列參數

```bash
pyinstaller --noconsole --onefile --name "餅乾庫存算料系統" ^
    --hidden-import gspread ^
    --hidden-import google.oauth2.service_account ^
    --hidden-import google.auth ^
    --hidden-import pyodbc ^
    --hidden-import tkinter ^
    --hidden-import tkinter.ttk ^
    --hidden-import threading ^
    --hidden-import cookies_inventory ^
    --hidden-import cookies_inventory.sync_utils ^
    --hidden-import cookies_inventory.sync_index_from_erp ^
    --hidden-import cookies_inventory.sync_inventory_from_erp ^
    --hidden-import cookies_inventory.sync_wip_from_erp ^
    --hidden-import cookies_inventory.sync_production_schedule ^
    --hidden-import cookies_inventory.sync_receipt_from_erp ^
    --hidden-import cookies_inventory.calculate_cookie_inventory ^
    --hidden-import cookies_inventory.google_sheets_helper ^
    --hidden-import cookies_inventory.erp_db_helper ^
    --hidden-import cookies_inventory.cookie_inventory_gui ^
    cookie_inventory_gui.py
```

**注意：** `config.ini` 和 `service_account.json` 不應使用 `--add-data` 打包，這些檔案包含機密資訊，需要使用者自行提供。

## 打包後的檔案結構

打包完成後，`dist/` 目錄中會產生：
- `餅乾庫存算料系統.exe` - 可執行檔案（GUI 圖形界面程式）

**打包的模組包含：**
- `cookie_inventory_gui.py` - GUI 主程式（根目錄）
- `cookies_inventory/__init__.py` - 套件初始化
- `cookies_inventory/sync_utils.py` - 同步模組共用工具（⭐ 新增）
- `cookies_inventory/sync_index_from_erp.py` - 同步 Index 資料
- `cookies_inventory/sync_inventory_from_erp.py` - 同步帳上庫存資料
- `cookies_inventory/sync_wip_from_erp.py` - 同步在製品庫存
- `cookies_inventory/sync_receipt_from_erp.py` - 同步完工入庫
- `cookies_inventory/sync_production_schedule.py` - 同步生產排程
- `cookies_inventory/calculate_cookie_inventory.py` - 計算庫存預估
- `cookies_inventory/google_sheets_helper.py` - Google Sheets 操作
- `cookies_inventory/erp_db_helper.py` - ERP 資料庫連接
- `cookies_inventory/cookie_inventory_gui.py` - GUI 界面實作
- 所有必要的 Python 套件和依賴

## ⚠️ 重要：機密檔案不會被打包

**基於安全考量，`config.ini` 和 `service_account.json` 這兩個包含機密資訊的檔案不會被打包進 exe。**

### 使用者需要自行提供的檔案

分發給使用者時，需要提供以下檔案：

1. **`餅乾庫存算料系統.exe`** - 主程式
2. **`config.ini`** - 資料庫連線設定檔（包含密碼等機密資訊）
3. **`service_account.json`** - Google 服務帳戶憑證（包含私鑰等機密資訊）

### 檔案放置位置

將這三個檔案放在**同一個目錄**下，例如：
```
餅乾庫存算料系統/
├── 餅乾庫存算料系統.exe
├── config.ini
└── service_account.json
```

### 範本檔案

專案中提供了範本檔案供參考：
- `config.ini.template` - 設定檔範本
- `service_account.json.template` - 憑證檔案範本

**注意：範本檔案中的 `YOUR_*` 需要替換為實際值**

## 注意事項

1. **設定檔和憑證檔案**
   - `config.ini` 和 `service_account.json` **不會**被打包進 exe
   - 使用者必須將這兩個檔案放在 exe 同目錄下
   - 程式會從 exe 同目錄讀取這些檔案

2. **ODBC 驅動程式**
   - 目標電腦需要安裝 SQL Server ODBC 驅動程式
   - 可以從 Microsoft 官網下載：
     - ODBC Driver 17 for SQL Server
     - ODBC Driver 18 for SQL Server

3. **檔案大小**
   - 打包後的 exe 檔案可能會比較大（約 50-100 MB）
   - 這是因為包含了所有 Python 依賴套件

4. **防毒軟體**
   - 某些防毒軟體可能會誤報打包後的 exe 檔案
   - 這是 PyInstaller 打包工具的常見情況，可以加入白名單

## 測試打包後的程式

1. 在沒有安裝 Python 的電腦上測試
2. **必須**確保以下檔案都在同一個目錄：
   - `餅乾庫存算料系統.exe`
   - `config.ini`（**必須提供**）
   - `service_account.json`（**必須提供**）

3. 雙擊執行 `餅乾庫存算料系統.exe`

### 首次設定步驟

如果是新使用者，需要：

1. 複製 `config.ini.template` 為 `config.ini`
2. 編輯 `config.ini`，填入：
   - Google Sheets URL
   - ERP 資料庫連線資訊（伺服器、資料庫名稱、帳號、密碼）
   - SQL 查詢語句（如果需要自訂）

3. 複製 `service_account.json.template` 為 `service_account.json`
4. 編輯 `service_account.json`，填入：
   - 從 Google Cloud Console 取得的服務帳戶憑證資訊
   - 包含完整的 private_key、client_email 等資訊

5. 確保 Google Sheets 已授予服務帳戶存取權限

## 疑難排解

### 問題：執行時出現「找不到模組」錯誤

**解決方法：**
- 檢查 `build_exe.spec` 中的 `hiddenimports` 是否包含所有必要的模組
- 確認所有 Python 模組檔案都在專案目錄中
- 重新執行打包命令
- 如果缺少特定模組，在 `hiddenimports` 中加入該模組名稱

**常見缺少的模組：**
- `cookies_inventory.sync_utils` - 同步模組共用工具
- `cookies_inventory.sync_receipt_from_erp` - 同步完工入庫模組
- `cookies_inventory.sync_wip_from_erp` - 同步在製品庫存模組
- `tkinter` 或 `tkinter.ttk` - GUI 界面模組
- `threading` - 多線程模組

### 問題：無法連接到資料庫

**解決方法：**
- 確認目標電腦已安裝 SQL Server ODBC 驅動程式
- 檢查 `config.ini` 中的資料庫連線設定

### 問題：無法連接到 Google Sheets

**解決方法：**
- 確認 `service_account.json` 檔案正確
- 確認 Google Sheets 已授予服務帳戶存取權限

### 問題：程式執行緩慢

**解決方法：**
- 使用 `--onefile` 模式會較慢，因為需要解壓縮
- 可以改用目錄模式（移除 `--onefile`），但需要包含整個目錄

## 進階設定

### 自訂圖示

1. 準備一個 `.ico` 圖示檔案
2. 在 `build_exe.spec` 中修改：
   ```python
   icon='your_icon.ico',
   ```

### 減少檔案大小

可以排除不需要的模組：
```python
excludes=['matplotlib', 'numpy', 'pandas'],  # 如果不需要這些模組
```
